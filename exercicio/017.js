 const soma =  (v1, v2) => {
    console.log(v1 + v2);

 }
 soma(30,15);
 const subtracao =  (v1, v2) => {
    console.log(v1 - v2);

 }
 subtracao(10,5);
 const divisao =  (v1, v2) => {
    console.log(v1 / v2);

 }
 divisao(20,2);
 const multiplicacao =  (v1, v2) => {
    console.log(v1 * v2);

 }

 multiplicacao(4,5);