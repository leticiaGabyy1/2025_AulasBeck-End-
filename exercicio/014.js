const areaTriangulo = function (base, altura, largura){
    let area = (base * altura * largura) / 2;
    return area;
   
}

console.log(areaTriangulo(10, 15, 5));


const areaTriangulo1= (base, altura, largura) =>{
    let area = (base * altura * largura) / 2;
    return area;
   
}

console.log(areaTriangulo(10, 15, 5));
